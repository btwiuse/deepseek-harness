# Keep the build context small and reproducible: everything below is either
# regenerated by `pnpm install` / `pnpm run build` inside the image or is
# irrelevant to building the dsh CLI and web frontend.
.git
**/node_modules
**/lib
**/dist
**/*.tsbuildinfo
**/.cache
.env
*.log
coverage
